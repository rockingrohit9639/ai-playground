# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is an AI playground. Currently contains a `commit-lint` plugin skeleton under `claude-plugins/`.

## Structure

- `claude-plugins/<plugin-name>/.claude-plugin/plugin.json` — plugin metadata for each Claude plugin

## Plugin Development

Each plugin lives in `claude-plugins/<name>/` with a `.claude-plugin/plugin.json` manifest.
